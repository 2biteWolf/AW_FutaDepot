# AW_FutaDepot 0.1.45

Running Tk build. Start with START.bat or `py -3 ui.py`.

- Categories = folders under the library root (`_FutaMass` by default) plus ADD FOLDER
- No default dump path
- Run as-is always
- Silent only after scan (Inno / NSIS / MSI / depot.json)
- NET INSTALL = winget on Windows
- SCAN unblocks Zone.Identifier on Windows
- Language follows OS, toggle in header
- Updates from GitHub Releases via `channel/version.json`
- GitHub: https://github.com/2biteWolf/AW_FutaDepot

